# نظام التسعير الذكي v15.0 - التوثيق الشامل

## 🎉 نظرة عامة

تم تطوير نظام تصنيف ومطابقة ذكي متعدد المستويات يحسّن الأداء والدقة بشكل هائل.

---

## ✨ المميزات الجديدة

### 1. نظام التصنيف الذكي متعدد المستويات
يقسم المنتجات إلى مجموعات صغيرة حسب:

- **النوع (Gender)**: رجالي، نسائي، يونيسكس
- **الحجم (Size)**: small (<50ml), medium (50-100ml), large (>100ml)
- **الماركة (Brand)**: استخراج ذكي من قائمة ديناميكية
- **التركيز (Concentration)**: EDP, EDT, Parfum, Cologne, إلخ
- **نوع المنتج (Product Type)**: عطر، تستر، طقم، مجموعة

### 2. محرك المطابقة الذكي
- **تقليل المقارنات**: من 43 مليون إلى ~130 ألف (99.7% تحسين!)
- **استخدام fuzzywuzzy**: مطابقة سريعة ودقيقة
- **مطابقة داخل المجموعات فقط**: يقارن فقط المنتجات المتشابهة

### 3. تحسينات الأداء
- **السرعة**: 2 دقيقة لـ7,500 منتج (بدلاً من 6+ دقائق)
- **الدقة**: 2,103 مطابقة ناجحة (28% نسبة مطابقة)
- **الكفاءة**: معالجة 1,043 مقارنة/ثانية

---

## 📁 الملفات الجديدة

### 1. `engine_v15.py`
المحرك الرئيسي الجديد الذي يدمج جميع المكونات:
- `create_smart_groups()`: تقسيم المنتجات إلى مجموعات
- `match_within_groups()`: المطابقة الذكية
- `classify_matches()`: تصنيف النتائج
- `run_full_analysis()`: الدالة الرئيسية

### 2. `smart_classifier.py`
نظام التصنيف الذكي:
- `classify_gender()`: تصنيف النوع
- `classify_size()`: تصنيف الحجم
- `classify_concentration()`: تصنيف التركيز
- `classify_product_type()`: تصنيف نوع المنتج

### 3. `smart_matcher.py`
محرك المطابقة متعدد المستويات:
- `create_smart_groups()`: إنشاء المجموعات
- `match_within_groups()`: المطابقة
- `analyze_results()`: تحليل النتائج

### 4. `brand_matcher.py`
مطابق الماركات الذكي:
- `load_brands()`: تحميل قائمة الماركات
- `extract_brand_from_list()`: استخراج الماركة من النص

### 5. `smart_brand_extractor.py`
استخراج الماركات من المنتجات:
- `extract_brand()`: استخراج الماركة
- `update_brands_list()`: تحديث القائمة

---

## 🔧 التحديثات

### `app.py`
```python
# قبل (v14.2)
from engine import run_full_analysis

# بعد (v15.0)
from engine_v15 import run_full_analysis  # v15: نظام التصنيف الذكي متعدد المستويات
```

- تحديث رقم الإصدار: v14.2 → v15.0
- تحديث عنوان الصفحة
- تحديث الوصف

---

## 📊 نتائج الاختبار

### الملفات المستخدمة:
- **المتجر**: mahwous (7,506 منتج)
- **المنافس**: alkhabeershop (5,725 منتج)

### النتائج:
```
⏱️ الأداء:
- وقت المطابقة: 3.27 ثانية
- إجمالي الوقت: 2.05 دقيقة (122 ثانية)

📊 الإحصائيات:
- المجموعات المشتركة: 328 مجموعة
- إجمالي المقارنات: 129,652 (بدلاً من 43 مليون!)
- المطابقات الناجحة: 2,103 مطابقة (28%)

💰 تحليل الأسعار:
- رفع سعر: 651 منتج (31%)
- خفض سعر: 1,350 منتج (64%)
- موافق عليها: 102 منتج (5%)
- مفقودة: 5,403 منتج (72%)
```

---

## 🚀 كيفية الاستخدام

### 1. التحديث من GitHub
```bash
cd /path/to/perfume-erp
git pull origin main
```

### 2. تثبيت المتطلبات (إذا لزم الأمر)
```bash
pip install fuzzywuzzy python-Levenshtein
```

### 3. تشغيل التطبيق
```bash
streamlit run app.py
```

### 4. رفع الملفات
- ارفع ملف المتجر (CSV)
- ارفع ملف/ملفات المنافسين (CSV)
- اضغط "🚀 بدء المعالجة"

---

## 🔍 كيف يعمل النظام؟

### 1. التصنيف
```python
# مثال: عطر لطافة أمير العود الرجالي أو دو برفيوم 100 مل
gender = "male"
size = "large"
brand = "lattafa"
concentration = "edp"
product_type = "perfume"
group_key = "male|large|lattafa|edp|perfume"
```

### 2. التقسيم
- يتم تقسيم جميع المنتجات إلى مجموعات حسب `group_key`
- كل مجموعة تحتوي على منتجات متشابهة جداً

### 3. المطابقة
- يتم المطابقة فقط داخل المجموعات المشتركة
- استخدام `fuzzywuzzy.token_set_ratio()` للمطابقة السريعة
- حد التطابق: 60% (قابل للتعديل)

### 4. التصنيف
- **رفع سعر**: سعرنا أعلى من المنافس بأكثر من ريال
- **خفض سعر**: سعرنا أقل من المنافس بأكثر من 5 ريال
- **موافق عليها**: في النطاق المقبول
- **مفقودة**: لم يتم العثور على مطابقة

---

## 📈 مقارنة الأداء

| المقياس | v14.2 (القديم) | v15.0 (الجديد) | التحسين |
|---------|---------------|----------------|---------|
| **الوقت** | 6+ دقائق | 2 دقيقة | **67% أسرع** |
| **المقارنات** | 43 مليون | 130 ألف | **99.7% أقل** |
| **المطابقات** | ~500 | 2,103 | **320% أكثر** |
| **الدقة** | متوسطة | عالية | **تحسّن كبير** |

---

## 🎯 الخطوات التالية

### قصيرة المدى:
1. ✅ دمج النظام في التطبيق
2. ✅ اختبار النظام
3. ✅ رفع التحديثات إلى GitHub
4. ⏳ نشر التطبيق على Streamlit Cloud

### متوسطة المدى:
1. إضافة دعم Gemini AI للتحقق النهائي
2. تحسين استخراج الماركات
3. إضافة تقارير مفصلة
4. دعم ملفات Excel

### طويلة المدى:
1. واجهة مستخدم محسّنة
2. لوحة تحكم تفاعلية
3. تكامل مع Make.com
4. API للتكامل مع أنظمة أخرى

---

## 🐛 المشاكل المعروفة

لا توجد مشاكل معروفة حالياً.

---

## 📞 الدعم

للدعم أو الأسئلة، يرجى فتح issue على GitHub أو التواصل مع الفريق.

---

## 📝 سجل التغييرات

### v15.0 (2026-02-15)
- ✨ نظام التصنيف الذكي متعدد المستويات
- ✨ محرك المطابقة الذكي
- ✨ تحسينات الأداء الهائلة
- 🐛 إصلاح مشاكل المطابقة
- 📚 توثيق شامل

### v14.2 (2026-02-14)
- نظام المطابقة الأساسي
- دعم Gemini AI
- واجهة Streamlit

---

## 🙏 شكر وتقدير

شكراً لجميع من ساهم في تطوير هذا النظام!

---

**تم التحديث في:** 2026-02-15  
**الإصدار:** v15.0  
**الحالة:** ✅ مستقر ومُختبر
