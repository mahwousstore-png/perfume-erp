# إعداد Make.com - نظام التسعير الذكي

## 🎯 البنية المطلوبة

### Scenario 1: تحديث الأسعار

```
1. Webhooks → Custom Webhook
   - اسم: "استقبال تحديثات الأسعار"
   - احفظ الـ Webhook URL

2. Iterator
   - Array: {{1.data}}
   
3. Salla → Update Product
   - Product ID: {{2.No.}}
   - Price: {{2.سعر المنتج}}
   - Cost Price: {{2.سعر التكلفة}}
   - Sale Price: {{2.السعر المخفض}}
```

### Scenario 2: إضافة منتجات جديدة

```
1. Webhooks → Custom Webhook
   - اسم: "استقبال منتجات جديدة"
   - احفظ الـ Webhook URL

2. Iterator
   - Array: {{1.data}}
   
3. Salla → Create Product
   - Name: {{2.أسم المنتج}}
   - Price: {{2.سعر المنتج}}
   - Description: {{2.الوصف}}
   - SKU: {{2.رمز المنتج sku}}
   - Cost Price: {{2.سعر التكلفة}}
   - Weight: {{2.الوزن}}
   - Brand: {{2.الماركة}}
```

## 📋 بيانات الاختبار

### تحديث سعر منتج واحد:

```json
{
  "type": "price_update",
  "timestamp": "2026-02-10T12:00:00",
  "total_items": 1,
  "data": [
    {
      "No.": "115871054",
      "النوع": "منتج",
      "أسم المنتج": "تستر عطر لانكوم لا في إي بيل ل'إكستريت أو دو بارفيوم 50 مل",
      "رمز المنتج sku": "LANCOME-50ML",
      "سعر المنتج": 350,
      "سعر التكلفة": 200,
      "السعر المخفض": 320,
      "تاريخ بداية التخفيض": "",
      "تاريخ نهاية التخفيض": ""
    }
  ]
}
```

### إضافة منتج جديد واحد:

```json
{
  "type": "new_product",
  "timestamp": "2026-02-10T12:00:00",
  "total_items": 1,
  "data": [
    {
      "النوع": "منتج",
      "أسم المنتج": "عطر تجريبي جديد",
      "تصنيف المنتج": "عطور",
      "صورة المنتج": "https://example.com/image.jpg",
      "نوع المنتج": "منتج جاهز",
      "سعر المنتج": 100,
      "الوصف": "وصف العطر هنا",
      "هل يتطلب شحن؟": "نعم",
      "رمز المنتج sku": "SKU-123",
      "سعر التكلفة": 50,
      "السعر المخفض": 90,
      "الوزن": 0.2,
      "وحدة الوزن": "kg",
      "الماركة": "ديور",
      "الباركود": "123456789",
      "خاضع للضريبة ؟": "نعم"
    }
  ]
}
```

## 🧪 خطوات الاختبار

### 1. اختبار تحديث سعر واحد:

```bash
curl -X POST "YOUR_WEBHOOK_URL_1" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "price_update",
    "data": [{
      "No.": "115871054",
      "سعر المنتج": 350,
      "سعر التكلفة": 200
    }]
  }'
```

### 2. اختبار إضافة منتج واحد:

```bash
curl -X POST "YOUR_WEBHOOK_URL_2" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "new_product",
    "data": [{
      "أسم المنتج": "عطر تجريبي",
      "سعر المنتج": 100,
      "رمز المنتج sku": "TEST-001"
    }]
  }'
```

### 3. اختبار النظام الكامل:

- ارفع ملفات على التطبيق
- شغّل التحليل مع Gemini
- راجع النتائج
- اضغط "إرسال إلى Make.com"
- تحقق من سلة

## 🔗 الروابط المطلوبة

بعد إنشاء السيناريوهات، احفظ:

1. **Webhook URL 1** (تحديث الأسعار): `_______________`
2. **Webhook URL 2** (منتجات جديدة): `_______________`

ضعها في التطبيق:
- الشريط الجانبي → Make.com → Webhook URL

## ✅ التحقق من النجاح

### في Make.com:
- ✅ السيناريو يعمل بدون أخطاء
- ✅ البيانات تصل بشكل صحيح
- ✅ Salla يستقبل التحديثات

### في سلة:
- ✅ الأسعار محدثة
- ✅ المنتجات الجديدة مضافة
- ✅ جميع البيانات صحيحة

## 🚨 استكشاف الأخطاء

### خطأ: "Product not found"
- تأكد من صحة معرف المنتج (No.)
- تأكد من وجود المنتج في سلة

### خطأ: "Invalid price"
- تأكد من أن السعر رقم صحيح
- تأكد من عدم وجود قيم فارغة

### خطأ: "Connection failed"
- تأكد من صحة اتصال Salla في Make.com
- تأكد من صلاحيات API
