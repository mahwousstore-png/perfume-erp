# إصلاح: إضافة أسماء المنافسين للمنتجات المفقودة

## 📋 المشكلة
عند رفع ملفات المنافسين، كان النظام يعرض المنتجات المفقودة (الموجودة عند المنافسين وليست في متجرك) لكن **بدون معرفة من أي منافس جاء كل منتج**.

## ✅ الحل المطبق

### 1. التعديلات في `engine.py`

#### أ) إضافة حقل المنافس عند قراءة الملف (السطور 513-515)
```python
# إضافة اسم المنافس لكل منتج
for p in comp_products:
    p["_competitor_name"] = comp_file["name"]
```

#### ب) تمرير اسم المنافس للمنتجات المفقودة (السطر 381)
```python
"competitor_name": cp.get("_competitor_name", "غير محدد"),
```

#### ج) إضافة عمود المنافس في DataFrame (السطر 602)
```python
"المنافس": m.get("competitor_name", "غير محدد"),
```

### 2. التعديلات في `app.py`

#### أ) تحديث عرض المنتجات المفقودة (السطور 1160-1176)
- تغيير تقسيم الأعمدة من `[0.4, 3.5, 1.5, 1.5]` إلى `[0.3, 2.5, 1.0, 0.8, 1.4]`
- إضافة عمود خامس لعرض اسم المنافس
- إزالة امتداد الملف (.xlsx, .csv) لعرض نظيف
- اختصار الاسم إلى 15 حرف لتوفير المساحة

#### ب) إضافة جدول كامل للمراجعة (السطور 1213-1216)
```python
# عرض الجدول الكامل للمراجعة
st.markdown("---")
st.markdown("### 📊 جدول المنتجات المفقودة")
st.dataframe(df_missing, use_container_width=True, height=400)
```

## 🎯 النتيجة

الآن عند رفع ملفات المنافسين:
1. ✅ يتم عرض اسم المنافس بجانب كل منتج مفقود
2. ✅ يمكن معرفة من أي منافس جاء المنتج
3. ✅ يتم عرض جدول كامل في نهاية القسم للمراجعة
4. ✅ يتم تصدير اسم المنافس في ملف Excel عند التحميل

## 📦 الملفات المعدلة
- `engine.py`: 3 تعديلات (إضافة حقل المنافس، تمريره، عرضه)
- `app.py`: 2 تعديلات (تحديث العرض، إضافة الجدول)

## 🚀 الخطوات التالية
1. اختبار النظام برفع ملفات منافسين حقيقية
2. التأكد من ظهور أسماء المنافسين بشكل صحيح
3. التحقق من تصدير Excel يتضمن عمود المنافس

## 📝 ملاحظات
- تم الحفاظ على سياسة "صفر حذف" - جميع التعديلات إضافية فقط
- تم إنشاء نسخة احتياطية: `engine.py.backup_20260212_190802`
- تم رفع التعديلات إلى GitHub على branch `main`
- Branch الميزة: `feature/competitor-names-fix` (تم دمجه)

---
**التاريخ:** 2026-02-13  
**الإصدار:** v8.0.1  
**الحالة:** ✅ مكتمل ومرفوع
